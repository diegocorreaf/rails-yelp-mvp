<h1>Restaurants</h1>

<ul>
  <% @restaurants.each do |restaurant| %>
    <li>
      <%= link_to restaurant.name, restaurant_path(restaurant.id) %> | <%= restaurant.category %> - <%= restaurant.address %>
    </li>
  <% end %>
</ul>

<%= link_to "New Restaurant", new_restaurant_path %>